{{!-- <div class="row">
  <h3 class="card">{{article.title}}</h3>
  <h5 class="card-panel articleBody">{{body}}</h5>
</div>

<div class="row">
  <div class="col s2"></div>
  {{!-- // add form to add post request and use article id --}}
  <form action="/note/{{article._id}}" method="POST" class="card-panel col s8">
  <h5>Make a Note</h5>
  <div class="row">
{{!-- name input field --}}
<div class="input-field">
  <i class="material-icons prefix">Make Note</i>
  <input id="icon_telephone" type="text" placeholder="Make Note" name="note" class="validate active">
</div>

  </div>
  {{!-- comment --}}
  <div class="row">
<div class="input-field">
  <center><input class="btn add-comment-button topnav" type="submit" value="Submit"></center>
</div>
  </div>
  </form>
</div>
<div class="row">
  <div class="col s2"></div>
   <div class="col s8">
     <h5>Notes:</h5>
     {{#if article.note}}
     {{#each article.note}}
     <div class="card">
       <p id="nameNote">{{name}}</p>
       <p>{{body}}</p>
       </div>
       {{/each}}
       {{else}}
       <p>No Notes Yet!</p>
       {{/if}}
     </div>
   </div> --}}
